export default function Root(props) {
  //console.log(props);
  //return <section>{props.name} {JSON.stringify(props)} is mounted! TARGETS !!!</section>;
  return <section>{props.name} is mounted! <b>TARGETS !!!</b></section>;
}
